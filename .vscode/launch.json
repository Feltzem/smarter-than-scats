{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run app in default browser",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev -- --open",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Build app"
    }
  ]
}
